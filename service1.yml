apiVersion: v1
kind: Service

metadata:
  name: nginx-service-1
  namespace: nginx-2

spec:
  type: NodePort

  selector:
    app: nginx

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
